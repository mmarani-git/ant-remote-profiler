db is created with the auto-ddl update feature of hibernate.
Here I commit some useful view.
removed.sql contains stuff that is no more working because the project as changed: today, a view on tasks but I have removed task management